#include <iostream>
using namespace std;
int PeakElemet(int arr[], int n)
{
    int start = 0;
    int end = n - 1;
    int mid = start + (end - start) / 2;
    while (start <= end)
    {
        if (int arr[mid])
    }
}
int main()
{
}